{
  "name": "school-camp-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx prisma db push --force-reset && prisma generate && tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.0.0",
    "telegraf": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
